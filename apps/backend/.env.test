# Test environment variables
# These are dummy values for unit tests - NOT real credentials

NODE_ENV=test
PORT=3001

# Database (dummy URL for tests - actual connections are mocked)
DATABASE_URL=postgresql://test:test@localhost:5432/test_db?sslmode=disable

# Redis (dummy URL for tests - actual connections are mocked)
REDIS_URL=redis://localhost:6379

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Spotify OAuth (dummy values for tests)
SPOTIFY_CLIENT_ID=test_client_id_12345
SPOTIFY_CLIENT_SECRET=test_client_secret_67890

# Encryption key (64 hex characters = 32 bytes)
# Generated for testing purposes only - never use in production
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Circuit Breaker defaults
SPOTIFY_CB_THRESHOLD=5
SPOTIFY_CB_RESET_TIMEOUT=30000
SPOTIFY_CB_WINDOW_DURATION=60000
